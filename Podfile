# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BaseballSwift' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BaseballSwift
  pod 'Stripe'
  
  target 'BaseballSwiftTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'BaseballSwiftUITests' do
    # Pods for testing
  end

end
